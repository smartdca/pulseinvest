# HANDOFF — 浮動 DCA Score 工具獨立化 ＋ 公式收口

> 這份文件的存在理由只有一個：**防止終點在幾輪之後脫鉤。**
>
> 底下每一個階段單獨看都合理，但只有照順序走、朝同一個終點走，最後那一步才做得成。
> 任何一輪如果只顧眼前那一步，很容易做出之後要拆掉的東西。
>
> 開工前請一併讀 `HANDOFF_工作交接.md`（工作方式、站台硬規則、iOS 地雷）。

---

## 一、終點

**關掉 `api/score` 對外的門。**

### 現在漏在哪裡

`api/score` 是公開的，不需要通過任何驗證，而且它接受**任意編造的輸入**。
它不是被問「AAPL 幾分」，是被問「這五個數字換算出來是幾分」，而且有問必答。

這代表有人可以透過反覆送入自己指定的數字、比對回傳結果的差異，
把五個因子的比重逐一還原出來，得到的是精確值而不是猜測。
回傳內容裡同時附帶倍數與紅黃綠燈，所以 60 分那條線、各燈號的切點也會一起流出去。

需要的技術門檻很低，時間成本大約半天。

### 為什麼加鎖沒有用

問題不在「有沒有驗證」，在**結構**：
只要前端還在「算好五個百分位、再拿去問後端」，這道門就必須開著。
加驗證只是提高門檻，不是杜絕。

### 唯一的杜絕方式

**前端只送代號，其餘全部在後端完成。**

「AAPL 幾分」問一萬次也套不出公式，因為問的人無法指定輸入。

這個模式**已經存在**，就是 `api/ticker-score`，文章的 DCA Score chip 用了很久。
不需要開發新東西，是把首頁也改走同一條路。

### 附帶好處

| 項目 | 說明 |
|---|---|
| 單一分數來源 | 不會再出現兩邊算出不同分數。先前發生過，最大差 18 分，足以讓 60 分門檻翻掉 |
| 空出函式額度 | Vercel Hobby 目前 12/12 滿載，關掉對外入口後可回收一格 |

---

## 二、四個階段（依序，不可跳）

### 階段 1｜浮動快速查詢改走 `ticker-score`，同時包成獨立元件　**已完成**

原本規劃為兩階段（先改資料來源、再抽成獨立檔）。Henry 要求「一開始就做成可以隨時加」，
兩階段合併為一次，少一輪改動、也少一次回歸機會。

**做了什麼**

- 新增 `js/quick-score.js`：面板 HTML 由它自己注入，自帶中英文案，單次呼叫 `api/ticker-score`
- `index.html` 移除面板標記與整組函式，改成載入一行
- `api/ticker-score.js` 的預設回應加上 `price` / `currency`（加欄位，chip 不受影響）
- 首頁「今日一杯」呼叫的是同一支 `quickCalculateScore`，一併變輕，不需另外處理

**掛載方式**

任一頁加上載入 `js/quick-score.js` 的 script 標籤就有這個功能，移掉就沒有。
目前只有 `index.html` 掛著。

**相依：有就用、沒有就降級**

| 相依 | 缺席時 |
|---|---|
| `pickArcGauge`（`js/ui-widgets.js`） | 不畫儀表，其餘照常 |
| `getLogoUrl` / `autoLookupLogo`（`js/logo.js`） | 顯示代號首字母 |
| `acSearch`（首頁） | 退回純輸入框，自行輸入完整代號一樣查得到 |
| `switchTab` / `calculate`（首頁） | 「完整分析」改成跳回首頁帶代號，由首頁自動開始計算 |

---

### 階段 2｜其他頁面逐頁裝回

一頁一行，一頁一驗。哪一頁要就加，不要就不加。

**不要一次裝多頁。** 一次裝多頁，出問題分不清是元件本身還是那一頁的環境。

背景：這幾支頁面本來都有計算功能，是在拆成獨立網址的過程中失去的。
當初的計畫就是**全部拆完之後，再決定誰要裝回來**。這一階段就是那個「裝回來」。

---

### 階段 3｜首頁完整分析改走同一條

**這是整件事動作最大的一步。** index.html 是多對話框共用檔案，動它前務必請 Henry 下載當下線上版本當基底。

**未解問題（見第四節），必須先做一輪設計討論，不可直接動手。**

---

### 階段 4｜關掉 `api/score` 對外的門

階段 3 完成後，`api/score` 不再有任何前端呼叫端，
改成只讓自家後端內部呼叫，對外不再開放入口。

**外部嵌入版排在階段 4 之後，不在本文件範圍。**

---

## 三、已查證的事實

以下都是實際讀過程式碼確認的，**不要重查，也不要憑印象推翻**。
要推翻請先請 Henry 提供當下的線上檔案。

### 關於流程

| 事實 | 影響 |
|---|---|
| `goToFullAnalysis()` 只帶代號回首頁重算，浮動查詢算出的分數當場丟棄 | 改成輕的，這條路徑一個字都不會變 |
| 首頁熱搜泡泡已經在走 `ticker-score`，且不帶驗證 | 輕的那條路早就在站上運作 |

### 關於 `ticker-score`

| 事實 | 影響 |
|---|---|
| 預設（chip）路徑回傳 score / triggered / blackSwan，本輪加上 price / currency | 浮動查詢一次請求就拿到需要的四樣，不必走 `full=1` |
| `full=1` 另外回傳 multiplier / 五因子細項 / basic / crypto，會多打 FMP 與 Sheets | 資產頁用；浮動查詢刻意不用，維持最輕 |
| 預設路徑的回應被 CDN 快取 1 小時，價格因此最多是一小時前的 | 分數本來就同樣快取；DCA 情境下可接受，且與 chip 一致 |
| 與 `api/score` 共用同一份 `calcScore` | 分數必然一致 |
| CDN 邊緣快取 1 小時；降級結果 5 分鐘；失敗不快取 | 同一代號第二個訪客不會執行到函式 |
| IP 每分鐘 30 次限制（已知限制：跨 instance 不共用，實際擋不住分散流量） | 自家夠用，外部版要重新評估 |
| CORS 寫死只接受 `https://dcacafe.com` | **外部嵌入版必須重新設計，不能單純放寬。** 放寬等於誰都能無限制打後端 |
| P/FCF 走季快取＋股價比例還原，一支股票一季只碰一次 FMP | FMP 每日 250 次額度不會因為流量爆掉 |

### 關於 index.html（行號以本輪基準檔為準，動檔前重新確認）

| 事實 | 影響 |
|---|---|
| 計算引擎在 2672–3003，完全不碰 DOM | 階段 3 若要抽出，切點乾淨 |
| `AGE_CAPS` 2496、`getAgeBand` 2526、`getAgeCaps` 2533 | 引擎的依賴，要一起處理 |
| `const PROXY` 1157、`let turnstileToken` 1183 | **新檔若再宣告一次 `const PROXY` 會重複宣告，整支檔案不執行。** 新檔一律用 IIFE 包起來，再明確掛上 `window` |
| `PROXY` 另有一處給自動完成用（1630） | 不能因為引擎搬走就刪掉 |
| 全檔 5312 行，71 處日期字串 | 違反「任何檔案不出現日期」硬規則，待獨立處理，不要混進本計畫 |

### 現行重路徑的成本（改掉的就是這個）

一次浮動查詢：10 年日線 + 5 年 VIX + 財報資料 + `api/score`，共 3～4 次外部請求，
且必須通過 Turnstile。Turnstile 綁定自家網域，這條路結構上不可能離開 dcacafe.com。

---

## 四、階段 3 的未解問題

**這幾項在階段 1～2 不要擅自決定，留到階段 3 的設計討論。**

| 問題 | 說明 |
|---|---|
| 年齡上限 | 倍數上限依年齡分三段，年齡存在使用者本機，後端看不到 |
| VIX 加成 | 目前在前端疊加 |
| 股息加成 | 目前在前端疊加 |
| 走勢圖原始價格 | `ticker-score` 不回傳每日價格，這一塊仍需前端自行取得 |

結果會是「一部分搬到後端、一部分留在前端」，接縫處是風險最高的地方。

---

## 五、判斷準則

遇到取捨時用這三條：

1. **任何新做的東西，如果讓「前端送百分位給後端」這件事多一處，就是走錯方向。**
2. **分數只能有一個來源。** 出現第二套算法就是退步，不管理由多合理。
3. **階段 3 之前不動首頁的 `calculate()`。**

---

## 六、進度

| 階段 | 狀態 |
|---|---|
| 1　浮動查詢改走 `ticker-score`，包成 `js/quick-score.js` | 已交付，待實機驗收 |
| 2　其他頁面逐頁裝回 | 未開始 |
| 3　首頁完整分析改走同一條 | 未開始（需先做設計討論） |
| 4　關掉 `api/score` 對外入口 | 未開始 |
| —　外部嵌入版 | 階段 4 之後，不在本範圍 |

## 七、Logo 來源更換（本輪完成一半）

### 為什麼換

舊做法是兩步:先問第三方 allinvestview「這個代號的公司網域」,拿到網域再去
cdn.tickerlogos.com 抓圖。兩步都會失敗,而且失敗很安靜(掉到文字後備,不報錯)。

實測舊來源的問題:

| 測試 | 結果 |
|---|---|
| AAPL | 查不到。對方回的網址是 `www.apple.com`,沒有 http 開頭,被程式裡一條「網址必須含 http」的判斷擋掉 |
| BTC | 撈到一家同名的澳洲上市公司 |
| 0050.TW | `count: 0`,完全沒有資料 |
| MNST | 查不到,所以首頁本週精選主打卡長期沒有 Logo |

### 換成什麼

Brandfetch Logo API。免費、不需要標註來源、網址直接帶代號,不需要先查網域。

```
https://cdn.brandfetch.io/ticker/{代號}/w/{px}/h/{px}/fallback/404?c={client id}
https://cdn.brandfetch.io/crypto/{幣別}/w/{px}/h/{px}/fallback/404?c={client id}
```

Client ID(公開金鑰,設計上就是放在網頁裡的):`1id2WDtIC6Z6ifyEB9h`

**`fallback/404` 不可省略。** 不加的話查不到會回一張空白圖 —— 圖片算載入成功,
`onerror` 不會觸發,文字後備永遠不啟動,畫面上就是一塊白,比破圖還難看。

實測結果:AAPL、MNST、QQQ、GLD、BTC(crypto 路徑)都有正確的圖;
0050.TW 沒有 → 回 404 → 走文字後備。**台股要靠 `assets.js` 的 `DCA_LOGO_IMG` 自己指圖**,
這是結構問題(0050 沒有自己的公司網站),換任何供應商都一樣。

### 已完成

- `js/logo.js` 整支重寫:三層(本地圖 → Brandfetch → 文字後備),刪掉舊查詢與 localStorage 快取
- `index.html` 刪掉 60 幾筆的 `LOGO_DOMAIN_MAP`,並修正五處自己寫死的圖片樣式
- `css/main.css`、`css/web.css` 拿掉 logo 外框內距,圖改成貼滿到邊(自帶底色被圓角裁切)
- 精選卡 logo 尺寸放大對齊右側三行文字(小卡 46px、主打卡 52px,桌機主打卡維持 112px)
- `js/quick-score.js` 後備文字改取前四碼(原本只取一碼,0050.TW 顯示成孤零零一個「0」)

### 未完成 —— 下一輪的第一件事

**還有四支檔案各自抄了一份 logo 邏輯,尚未收進 `js/logo.js`:**

| 檔案 | 現況 |
|---|---|
| `jury.html` | 自己一份 `LOGO_DOMAIN_MAP` + **三份** `autoLookupLogo` |
| `confirm.html` | 自己一份 `LOGO_DOMAIN_MAP` + 一份 `autoLookupLogo` |
| `trending.html` | 直接從 `assets.js` 的 `domain` 組舊圖庫網址 |
| `scripts/asset-template.html` | 同上,兩處(hero 大 logo、底部相關卡) |

jury 那份對照表跟 index 那份**內容不一樣**(它多了 XRP、SOL、DOGE 等十幾個幣種),
所以同一支幣在不同頁面的 logo 行為本來就不同。這就是「有時好有時壞」的真正原因:
不是一個東西壞,是六種實作各自壞在不同地方。

**收的時候不只收查詢,顯示樣式也要一起收進 `createLogoImg()`。**
本輪踩過一次:把 `<img>` 行內的 padding 拿掉,結果 CSS 那條 `padding:4px` 反而生效,
圖變成「被推到中間的直角小圖」,比原本更明顯。同一個屬性寫在兩個地方就會這樣。

### 已解決:`check-links.py` 的漏洞

守門程式原本只檢查頁面之間的連結,不檢查 `script`/`link` 指向的檔案是否存在。
本輪發生過 `index.html` 已指向一支還沒上傳的 js、CI 全綠放行、線上 404 的情況。
第六條規則已補上,並用模擬 repo 驗過四種寫法不會誤報。

---

## 八、版號的實際規則

「全站版號一起跳」是簡化說法,真正的要求是:**所有載入那支檔案的頁面必須一起跳**。
少跳一頁,那一頁就跑舊程式,症狀是「A 頁好了、B 頁沒有」,看起來像隨機。

目前載入 `js/logo.js` 的只有 `index.html` 與 `backtest.html`(`zh/` 由 CI 產生)。

本輪的改動**沒有跳版號**,維持 `?v=5`。理由:跳號要動全站每一支帶版號的檔案,
而這幾次都不是破壞性改動,GitHub Pages 的快取本來就短,Purge 之後多數人會直接拿到新的。
下次若有破壞性改動,再一次跳齊。

---

## 九、待盤點與待討論

| 項目 | 說明 |
|---|---|
| 本週精選資產池 | Henry 想擴充資產數量並優化挑選規則。資料由 `update_picks.py` 產生成 `picks.json`,牽涉選股邏輯,值得單獨一輪 |
| 三支不明函式 | Vercel 上有 `alpaca`、`classify-purp…`、`like-calendar`,交接檔完全沒記錄,等於沒人看管卻占著額度 |
| 三支 preview 檔案 | `scale-preview.html`、`hero-preview.html`、`asset-desktop-mock.html` —— Henry 確認不用,本輪未動,可另行清理 |
| 跨 repo 污染 | 前端的 `js/logo.js` 曾被同時上傳到 Proxy 的 `api/logo.js`,放了兩個月沒人發現(它從來沒活過)。上傳時要確認 repo |

---

*硬規則、CI、連結與版號、iOS 地雷:見 `HANDOFF_工作交接.md` 與 `HANDOFF_連結與版號規則.md`。*
